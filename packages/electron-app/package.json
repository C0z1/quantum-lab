{
  "name": "quantum-lab",
  "version": "1.3.0",
  "description": "Quantum Lab — visualizador 3D interactivo de algoritmos cuánticos (Grover, Teletransportación, Shor, Deutsch-Jozsa, Bernstein-Vazirani, QFT)",
  "author": "C0z1",
  "license": "MIT",
  "main": "src/main/index.js",
  "scripts": {
    "bundle": "esbuild src/renderer/scene.js --bundle --format=esm --outfile=src/renderer/scene.bundle.js --platform=browser --log-level=warning",
    "dev": "npm run bundle && electron .",
    "start": "npm run bundle && electron .",
    "dist": "npm run bundle && electron-builder",
    "dist:win": "npm run bundle && electron-builder --win nsis",
    "lint": "eslint src",
    "format": "prettier --write \"src/**/*.{js,css,html}\"",
    "format:check": "prettier --check \"src/**/*.{js,css,html}\""
  },
  "dependencies": {
    "@fontsource/jetbrains-mono": "^5.3.0",
    "@fontsource/orbitron": "^5.3.0",
    "@fontsource/space-grotesk": "^5.3.0",
    "msgpackr": "^1.12.1",
    "three": "^0.164.1",
    "zeromq": "^6.8.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^8.0.0",
    "electron": "^30.5.1",
    "electron-builder": "^24.0.0",
    "esbuild": "^0.28.2",
    "eslint": "^10.11.0",
    "globals": "^17.12.0",
    "prettier": "^3.9.8"
  },
  "build": {
    "appId": "com.quantumlab.app",
    "productName": "Quantum Lab",
    "asar": false,
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "package.json",
      "node_modules/**/*"
    ],
    "extraResources": [
      {
        "from": "engine-bin",
        "to": "engine",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "target": [
        "nsis"
      ],
      "artifactName": "QuantumLab-Setup-${version}.exe"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "shortcutName": "Quantum Lab"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "category": "Science"
    },
    "mac": {
      "target": [
        "dmg"
      ],
      "category": "public.app-category.education"
    }
  }
}
